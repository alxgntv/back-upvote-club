# 🐍 Автоматическая активация виртуального окружения

Этот файл содержит инструкции по настройке автоматической активации виртуального окружения для папки backend.

## 📁 Структура файлов

- `.envrc` - Файл для direnv (автоматическая активация при входе в папку)
- `.zshrc` - Настройки для zsh терминала
- `activate_venv.sh` - Скрипт для ручной активации
- `.auto_activate` - Универсальный скрипт активации

## 🚀 Способы активации

### 1. Автоматическая активация с direnv (Рекомендуется)

```bash
# Установите direnv (если не установлен)
brew install direnv

# Разрешите direnv для этой папки
direnv allow

# Теперь при входе в папку backend виртуальное окружение активируется автоматически
cd /Users/alexign/Desktop/BB/backend
```

### 2. Автоматическая активация с zsh

```bash
# Добавьте в ваш ~/.zshrc:
if [[ -f "/Users/alexign/Desktop/BB/backend/.zshrc" ]]; then
    source "/Users/alexign/Desktop/BB/backend/.zshrc"
fi

# Или просто запустите:
source /Users/alexign/Desktop/BB/backend/.zshrc
```

### 3. Ручная активация

```bash
# Перейдите в папку backend
cd /Users/alexign/Desktop/BB/backend

# Запустите скрипт активации
source activate_venv.sh

# Или используйте универсальный скрипт
source .auto_activate
```

### 4. Алиасы для быстрого доступа

```bash
# Добавьте в ваш ~/.zshrc или ~/.bashrc:
alias backend="cd /Users/alexign/Desktop/BB/backend && source .auto_activate"
alias activate_backend="cd /Users/alexign/Desktop/BB/backend && source activate_venv.sh"
```

## 🔧 Настройка переменных окружения

Скрипты автоматически устанавливают следующие переменные:

- `DJANGO_SETTINGS_MODULE=buddyboost.settings`
- `PYTHONPATH` включает текущую папку backend

## 📋 Доступные команды

После активации виртуального окружения доступны следующие команды:

```bash
# Django команды
python manage.py runserver
python manage.py migrate
python manage.py collectstatic

# Установка зависимостей
pip install -r requirements.txt

# Деактивация виртуального окружения
deactivate
```

## 🐛 Устранение неполадок

### Виртуальное окружение не найдено

```bash
# Создайте виртуальное окружение в корневой папке проекта
cd /Users/alexign/Desktop/BB
python -m venv venv
source venv/bin/activate
pip install -r backend/requirements.txt
```

### Проблемы с правами доступа

```bash
# Сделайте скрипты исполняемыми
chmod +x /Users/alexign/Desktop/BB/backend/activate_venv.sh
chmod +x /Users/alexign/Desktop/BB/backend/.auto_activate
```

### Проверка активации

```bash
# Проверьте, что виртуальное окружение активировано
which python
echo $VIRTUAL_ENV
```

## 📝 Примечания

- Виртуальное окружение находится в `/Users/alexign/Desktop/BB/venv/`
- Все скрипты проверяют, что вы находитесь в папке backend
- Скрипты предотвращают повторную активацию уже активного окружения
- Цветной вывод помогает визуально определить статус активации
